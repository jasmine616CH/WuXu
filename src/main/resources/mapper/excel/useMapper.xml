<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.easyExcel.read.mapper.useMapper">

    <update id="batchBorrow" parameterType="java.util.List">
        update wuxu.object
        <foreach collection="list" item="item" separator=",">
            #{item.borrowing}
        </foreach>
        set state = 1,
            time = #{time}
        WHERE id IN
        <foreach collection="list" item="item" open="(" close=")" separator=",">
            #{item.id}
        </foreach>
    </update>

    <update id="batchBack" parameterType="java.util.List">
        update wuxu.object
        set state = 0,
        time = null,
        borrowing = null
        WHERE id IN
        <foreach collection="list" item="item" open="(" close=")" separator=",">
            #{item.id}
        </foreach>
    </update>

</mapper>